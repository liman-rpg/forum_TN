%p
  Questions list:

.questions-list
  %ul
    - @questions.each do |question|
      %li= render 'question_link', question: question

- if user_signed_in?
  %p= link_to "Add Question", new_question_path
