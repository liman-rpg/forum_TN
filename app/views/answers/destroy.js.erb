<% if @answer.destroyed? %>
  $('.notice').html("<%= flash[:notice] %>")
  $('.answers').find('#answer-id-<%= @answer.id %>').remove();
<% else %>
  $('.notice').html("<%= flash[:notice] %>")
<% end %>
